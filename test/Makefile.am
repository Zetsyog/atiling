################################################################################
# Tests

SUBDIRS = polybench

clean-local:
	find . -type f -iname "*.o" -delete
	find . -type f -iname "*_gen.c" -delete
	find . -type f -iname "*_gen*.h" -delete
